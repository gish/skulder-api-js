var connection = require('./../libs/mysql-connector.js'),
    transactions,
    _save;

_save = function() {
};

module.exports = {
    init: function(options) {
    },
    load: function(cb) {
    },
    add: function(data) {
    },
    delete: function(id) {
    }
};
